name: Build
on: [push]
jobs:
  Check build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3
      - name: Get submodules
        run: git submodule update --init --recursive
      - name: Run build
        run: ./build

